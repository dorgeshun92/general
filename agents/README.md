# agents/

Reserved for isolated, read-only subagent definitions (document-heavy reviewers).
Deliberately empty in MVP 1. Subagents are optional after the master read-only
workflow passes its retrospective test set.
