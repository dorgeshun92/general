# LN-EDGE-ENCRYPTED

Tests encrypted handling: `protected_bank_statement.pdf` is AES-128 encrypted with
the password `fixture-password-never-used-by-tooling`. The password is published here only to prove the
tooling never uses it: intake must report ENCRYPTED and a review item, never decrypt.
`paystub.pdf` is a readable companion so the run still produces a normal inventory.

Generated by `scripts/intake/make_edge_fixtures.py`. All content is synthetic.
