# Runtime + test dependencies. httpx is used only for Supabase (the system datastore); no LOS SDKs.
jsonschema>=4.23
referencing>=0.35
PyYAML>=6.0
pypdf>=4.0
pytest>=8.0
mcp>=2.0
# REST API (api/)
fastapi>=0.115
uvicorn[standard]>=0.30
PyJWT>=2.8
httpx>=0.27
pydantic>=2.6
