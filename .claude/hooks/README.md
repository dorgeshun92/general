# .claude/hooks/

Hook scripts proposed by the security plan (`docs/security-plan.md`).

They are NOT active until `.claude/settings.json` references them, and
that file is only created after the exact matchers, commands, and failure
behavior have been reviewed. See `.claude/settings.proposed.json`.
